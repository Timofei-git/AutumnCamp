# Для каждой из этих функций укажи в комментарии сложность: (а) функция с одним циклом по списку,
# (б) функция с двумя вложенными циклами, (в) функция, возвращающая arr[0], (г) функция, вызывающая sorted(arr).

# Задача 2
# базовый уровень
# Для каждой из этих функций укажи в комментарии сложность:
# (а) функция с одним циклом по списку, (б) функция с двумя вложенными циклами, (в) функция, возвращающая arr[0], (г) функция, вызывающая sorted(arr).

# a O(n)
# b O(n**2)
# в O(1)
# г O(nlogn)
